"""
violations_utils.py
Created: 2025-04-12

Helper utilities for working with violations data.
To be written with GitHub Copilot assistance.
"""

import pandas as pd

def filter_active_violations(df: pd.DataFrame) -> pd.DataFrame:
    """Return only active, non-dismissed violations."""
    pass

def group_by_borough(df: pd.DataFrame) -> pd.DataFrame:
    """Return counts of violations by boro."""
    pass

def sort_by_issue_date(df: pd.DataFrame, descending=True) -> pd.DataFrame:
    """Sort violations by ISSUE_DATE."""
    pass